# <Headline of the post>

**Status: draft. Not approved.** Sam approves, by name, in `publish.json`.

## Where it came from

`social/ideas/<file>.md`, or the reason it was invented — and confirm the idea
file now records this package under `used`.

## The image

| | |
|---|---|
| Source | `<library key>` → `social/library/<folder>/<file>` |
| Original | `<w>×<h>` |
| Crop | `<w>x<h>+<x>+<y>` — recorded because two posts from one staging are a grid problem even when the files differ |
| Feed | 1080 × 1350 |
| Story | 1080 × 1920 |
| Generated? | No / Yes — if yes, the reference used and the 1:1 check result |

Opened at full size after rendering and before the alt text was written.

## Claim table

| Claim | Verified against |
|---|---|
|  | `facts/<file>.md` (verified YYYY-MM-DD) |

An empty table is a real statement — it says this post makes no factual claim —
and it still gets written.

## Left out on purpose

What was cut, and why. This section is the most useful one in the file three
months later. ⚠ anything that needs Sam or head office.

## Consent

People in frame, client premises, third-party brands. "None" is a valid answer
and still gets written down.

## Alt text

> Written last, from the finished image.

## Compliance checklist

- [ ] No client name, logo or premises
- [ ] No agent gifting, no seasonal range
- [ ] No generated person presented as real
- [ ] No blocked claim (see the table in the skill)
- [ ] No superlative
- [ ] "allergy-free" in its approved wording, if used
- [ ] No place claim on out-of-town photography
- [ ] Alt text written from the finished image
- [ ] `source_images` recorded — present, not absent
- [ ] `node social/imagecheck.js` passes
- [ ] Approved by Sam
